<nav class="navbar-expand-lg navbar-light pt-3 pb-3">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
        aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon bg-light"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link text-white pr-3 pl-3 bg-dark" href="#">Home <span
                        class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white pr-3 pl-3 bg-dark" href="#">Saved</a>
            </li>
            <li class="nav-item">
                <a onclick="stop()" class="nav-link text-dark pr-3 pl-3 bg-light pulse" id="pulseButton"
                    href="/scrape"><span>Scrape Articles</span></a>
            </li>
        </ul>
        <span class="navbar-text text-white" style="font-size: 38px;">
            <h1>Mongo webscraper</h1>
        </span>
    </div>
</nav>
</div>

<div id="wrapper">
    <div id="articles">{{#each Article }}
        {{ this.title }}
        {{ this.link }}
        {{ this.paragraph }}
        {{/each}}</div>
    <div id="notes"></div>
